import { BsArrowLeftRight, BsArrowDownShort, BsSearch } from 'react-icons/bs'

interface Props {
  itemsCount: number,
  category: string
}

const SearchMenu: React.FunctionComponent<Props> = ({ itemsCount, category }) => {
    return (
        <div></div>
  )
}

export default SearchMenu;